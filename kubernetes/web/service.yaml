apiVersion: v1
kind: Service
metadata:
  name: django-node-port
  namespace: storage-manager

spec:
  type: NodePort

  selector:
    app: django

  ports:
    - name: http
      port: 8000
      targetPort: 8000
      nodePort: 30080
      protocol: TCP


