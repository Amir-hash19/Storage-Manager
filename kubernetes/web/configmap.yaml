apiVersion: v1
kind: ConfigMap
metadata:
  name: django-config
  namespace: storage-manager

data:
  DEBUG: "1"

  POSTGRES_DB: "storage_db"
  POSTGRES_USER: "admin"

  MINIO_ENDPOINT: "minio:9000"
  MINIO_BUCKET_NAME: "storage-manager"
  MINIO_USE_SSL: "False"




